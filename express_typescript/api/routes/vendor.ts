import { Router } from "express";

const vendorRouter = Router();

vendorRouter.get(":/slug", () => {});

vendorRouter.put("/:id", () => {});

vendorRouter.delete("/:id", () => {});

vendorRouter.post("/", () => {});

export default vendorRouter;
